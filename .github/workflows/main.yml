name: Check-affected

on:
  push:
    branches:
      - main

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        run : actions/checkout@v3

      - name: Install
        run : npm ci --legacy-peer-deps


